def main():
    li1 = [1,2,3,4,5]
    li2 = map(lambda x: x * x, li1)
    li3 = filter(lambda x: x%2==0, li1)
    print(*li1)
    print(*li2)
    print(*li3)

    func_plus = lambda x, y: x + y      #람다 함수를 func_plus로 정의
    print(func_plus(3, 4))

if __name__ == "__main__":
    main()